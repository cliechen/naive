version: '3'

services:
  naive:
    image: jonssonyan/naive
    container_name: naive
    restart: always
    network_mode: host
    volumes:
      - /naive:/naive
    environment:
      TZ: Asia/Shanghai